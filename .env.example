# chennix-api 环境变量示例
# 复制为 .env 并修改后使用：cp .env.example .env

# [可选] 管理员密码 — 仅用于 CI/自动化部署，跳过 Web 初始化向导
# 默认不设置：首次访问 Web 界面会引导你通过 /setup 页面创建管理员账号
# 如需自动创建（username 固定为 admin），取消注释并设置强密码：
# CHENNIX_ADMIN_PASSWORD=change-me-to-a-strong-password
